
export default function Layout({children}) {
  return (
    <div style={{height:'90vh', padding:'20px', display:'flex', flexDirection:'column', justifyContent:'center', alignItems:'center'}}>
      {/* <h1>contoh</h1> */}
      {children}
    </div>
  )
}
